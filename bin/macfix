#!/bin/sh
USAGE="""
Usage:
  macfix {dock | audio}
"""

case $key in
  dock)
    echo "Restarting dock..."
    killall Dock
    exit 0
    ;;
  audio)
    echo "Restarting audio services..."
    ps ax | grep 'coreaudio[d]' | awk '{print $1}' | kill -9
    ;;
  *)
    echo $USAGE
  ;;
esac
